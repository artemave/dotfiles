sessionname dev

shell bash

hardstatus lastline "%{= dR} %-Lw%50>%{= dG}%n%f* %{= dR}%t%+Lw%<"

#caption splitonly "%{= dC} %-Lw%50>%{= dR}%n%f* %{= dC}%t%+Lw%<"

screen -t sh

screen -t vim

screen -t irb

screen -t mysql

screen -t server

screen -t log

screen -t root

select 0

